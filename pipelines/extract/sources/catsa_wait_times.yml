name: catsa_wait_times
script: fetch_catsa_wait_times.py
args: []
schedule: "11-59/30 * * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 15
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "security-checkpoint wait bands for all 17 listed airports; pages refresh every two minutes"
rate_limit: "index page plus one request per airport (18 to one host); every 30 minutes keeps that polite"
licence: "CATSA website terms"
attribution_required: true
