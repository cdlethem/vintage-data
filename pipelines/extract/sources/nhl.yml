name: nhl
script: fetch_nhl.py
args: []
schedule: "25 */6 * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "schedule/score data; no published rate limit but doesn't need sub-minute polling outside live games"
rate_limit: "no published limit"
licence: "NHL official API — public"
attribution_required: false
