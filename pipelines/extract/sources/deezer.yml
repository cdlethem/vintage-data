name: deezer
script: fetch_deezer.py
args: []
schedule: "25 * * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "chart ranks shift continuously; hourly polling is generous"
rate_limit: "50 req/5s documented; self-impose well under that, charts don't need more than hourly polling"
licence: "unstated"
attribution_required: false
