name: imdb_processing_times
script: fetch_imdb_processing_times.py
args: []
schedule: "10 13 * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "contribution-processing queue depth + oldest pending item; page updates each business day"
rate_limit: "HTML page parse; no published limit"
licence: "IMDb non-commercial-use terms"
attribution_required: true
