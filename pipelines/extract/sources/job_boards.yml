name: job_boards
script: fetch_job_boards.py
args: ["greenhouse", "stripe"]
schedule: "15 6 * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "hiring doesn't change by the minute; poll daily at most (single example board — WATCHLIST is empty upstream)"
rate_limit: "no published limit; keep watchlist small and explicit"
licence: "public ATS board endpoints"
attribution_required: false
