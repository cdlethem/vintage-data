name: internet_archive
script: fetch_internet_archive.py
args: []
schedule: "13-58/15 * * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "genuinely continuous; new items observed added under a minute before the probe"
rate_limit: "unpublished; poll every few minutes given real upload volume, request only needed fields via fl[]"
licence: "per-item (varies by collection)"
attribution_required: true
