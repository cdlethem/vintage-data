name: gdelt_doc
script: fetch_gdelt_doc.py
args: []
schedule: "9-54/15 * * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "GDELT itself refreshes every 15 minutes"
rate_limit: "no published limit; keyless public endpoint"
licence: "GDELT — free for research/non-commercial use"
attribution_required: true
