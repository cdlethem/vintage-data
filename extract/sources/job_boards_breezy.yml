name: job_boards_breezy
dag_factory: job_boards
provider: breezy
script: fetch_job_boards.py
args: ["--providers", "breezy", "--max-per-board", "10000"]
schedule: "15 6 * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 60
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "Named-company Breezy boards add smaller employers and structured salary text; live-verified 2026-09-04; independent provider task; poll daily."
rate_limit: "public board endpoint; bounded concurrency; daily polling"
licence: "public ATS board endpoints"
attribution_required: false
