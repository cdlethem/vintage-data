name: sensor_community
script: fetch_sensor_community.py
args: []
schedule: "1-56/5 * * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local
run_summary:
  required: true

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "global air-quality/noise readings refresh every ~5 minutes"
rate_limit: "no published hard limit; requires descriptive User-Agent"
licence: "Sensor.Community — public, attribution via User-Agent contact"
attribution_required: false
