name: mlb_statsapi
script: fetch_mlb_statsapi.py
args: []
schedule: "20 */6 * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "schedule + probable pitchers change daily in the run-up to each game"
rate_limit: "no published limit"
licence: "MLB Advanced Media — personal/prototype use only, read their terms before going further"
attribution_required: true
