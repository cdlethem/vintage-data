name: avalanche_forecasts
script: fetch_avalanche_forecasts.py
args: []
schedule: "20 13 * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "SEASONAL (Nov-May); bulletins update once/twice a day, off-season returns 'no rating' (valid empty-ish result)"
rate_limit: "no published limit; public-safety non-profit/EAWS services, poll daily"
licence: "varies per EAWS member service; several require attribution to issuing warning service"
attribution_required: true
