name: job_boards_smartrecruiters
dag_factory: job_boards
provider: smartrecruiters
script: fetch_job_boards.py
args: ["--providers", "smartrecruiters", "--max-per-board", "10000"]
schedule: "15 6 * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 60
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "Named-company SmartRecruiters boards add global industrial, retail, hospitality and consumer employers; live-verified 2026-09-04; independent provider task; poll daily."
rate_limit: "public paginated endpoint; six concurrent requests maximum with a 100ms global request gap; daily polling"
licence: "public ATS board endpoints"
attribution_required: false
