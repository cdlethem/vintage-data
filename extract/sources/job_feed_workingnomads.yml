name: job_feed_workingnomads
script: fetch_job_feeds.py
args: ["workingnomads"]
schedule: "51 6 * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "Public exposed-jobs API; one full remote-job response daily. Live-verified 2026-09-04."
rate_limit: "one request daily"
licence: "public Working Nomads exposed jobs API"
attribution_required: true
