name: openligadb
script: fetch_openligadb.py
args: []
schedule: "50 */6 * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "Bundesliga (bl1) match data + table; community-run, be gentle"
rate_limit: "no published limit; volunteer-run"
licence: "OpenLigaDB — community project"
attribution_required: false
