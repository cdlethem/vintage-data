name: job_boards_recruitee
dag_factory: job_boards
provider: recruitee
script: fetch_job_boards.py
args: ["--providers", "recruitee", "--max-per-board", "10000"]
schedule: "15 6 * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 60
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "Named-company Recruitee boards add Benelux and Nordic SMEs with structured location and employment fields; live-verified 2026-09-04; independent provider task; poll daily."
rate_limit: "public board endpoint; bounded concurrency; daily polling"
licence: "public ATS board endpoints"
attribution_required: false
