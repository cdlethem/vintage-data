name: jolpica_f1
script: fetch_jolpica_f1.py
args: []
schedule: "40 7 * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "driver/constructor standings change after each race weekend, not continuously"
rate_limit: "volunteer-run on donations; published ToS, cache aggressively, never re-fetch completed races"
licence: "Jolpica-F1 — Ergast-compatible, respect ToS"
attribution_required: false
