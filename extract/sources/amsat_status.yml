name: amsat_status
script: fetch_amsat_status.py
args: []
schedule: "0-59/15 * * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "crowd-reported satellite reception reports; new reports arrive continuously"
rate_limit: "keyless AMSAT v1 API; poll every 15 minutes"
licence: "unstated"
attribution_required: false
