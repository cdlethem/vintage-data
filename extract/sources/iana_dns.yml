name: iana_dns
script: fetch_iana_dns.py
args: []
schedule: "55 6 * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 15
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "DNS root-zone snapshot (~2.2 MB); sibling dataset rdap via --dataset"
rate_limit: "poll daily; large file"
licence: "IANA file-use terms"
attribution_required: true
