name: ri_shelter_stats
script: fetch_ri_shelter_stats.py
args: []
schedule: "50 8 * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "current-month animal-shelter intake/outcome ledgers (aggregate, dog, cat, ferret, other) in one run"
rate_limit: "five report requests per run; daily is ample for cumulative monthly tables"
licence: "Rhode Island terms"
attribution_required: true
