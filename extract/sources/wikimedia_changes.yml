name: wikimedia_changes
script: fetch_wikimedia_changes.py
args: []
schedule: "*/10 * * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "en.wikipedia recentchanges poll; a continuous edit stream"
rate_limit: "no published limit for the Action API poll path"
licence: "Wikimedia — CC-BY-SA / GFDL"
attribution_required: false
