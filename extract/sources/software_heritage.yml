name: software_heritage
script: fetch_software_heritage.py
args: []
schedule: "45 */6 * * *"  # UTC
enabled: true
retries: 1
timeout_minutes: 10
sink: local

# Vetting metadata (documentation only, not read by the DAG factory)
cadence_note: "per-origin archival visits happen on Software Heritage's own crawl schedule; a real visit was dated the day before the probe"
rate_limit: "~120 req/hour documented; self-impose well under it"
licence: "unstated"
attribution_required: false
