# Confined pull-request reviewer protocol v2

Review the exact immutable admission, base, patch, PR head, executor report, and verification manifest. Read only; never modify the worktree, approve, merge, push, or apply. Return the exact protocol-v2 review result.
