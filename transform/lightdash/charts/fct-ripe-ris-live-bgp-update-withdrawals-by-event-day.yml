chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_ripe_ris_live_bgp_update_bgp_observed_at_day
          yRef:
            field: fct_ripe_ris_live_bgp_update_average_withdrawal_count
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Bgp observed at (Day)
      yAxis:
      - name: Mean observed withdrawal count
    layout:
      xField: fct_ripe_ris_live_bgp_update_bgp_observed_at_day
      yField:
      - fct_ripe_ris_live_bgp_update_average_withdrawal_count
  type: cartesian
description: Mean `withdrawal_count` per source event day, counting prefixes in each BGP UPDATE withdrawal
  array. Each update has equal weight and bursty withdrawals can be hidden by the average; absent event
  days are absent rather than zero.
metricQuery:
  dimensions:
  - fct_ripe_ris_live_bgp_update_bgp_observed_at_day
  exploreName: fct_ripe_ris_live_bgp_update
  filters: {}
  limit: 5000
  metrics:
  - fct_ripe_ris_live_bgp_update_average_withdrawal_count
  sorts:
  - descending: false
    fieldId: fct_ripe_ris_live_bgp_update_bgp_observed_at_day
  tableCalculations: []
name: Mean withdrawn-prefix count per event day
slug: fct-ripe-ris-live-bgp-update-withdrawals-by-event-day
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_ripe_ris_live_bgp_update_bgp_observed_at_day
  - fct_ripe_ris_live_bgp_update_average_withdrawal_count
tableName: fct_ripe_ris_live_bgp_update
version: 1
