chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_musicbrainz_release_observation_observed_at_day
          yRef:
            field: fct_musicbrainz_release_observation_average_track_count
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Observed at (Day)
      yAxis:
      - name: Average track count
    layout:
      xField: fct_musicbrainz_release_observation_observed_at_day
      yField:
      - fct_musicbrainz_release_observation_average_track_count
  type: cartesian
description: Mean `track_count` across release observations on each observation day. Each retained row
  has equal weight, so irregular polling can weight releases unevenly; this describes the observed payload
  rather than all MusicBrainz releases.
metricQuery:
  dimensions:
  - fct_musicbrainz_release_observation_observed_at_day
  exploreName: fct_musicbrainz_release_observation
  filters: {}
  limit: 5000
  metrics:
  - fct_musicbrainz_release_observation_average_track_count
  sorts:
  - descending: false
    fieldId: fct_musicbrainz_release_observation_observed_at_day
  tableCalculations: []
name: Average tracks per observed release
slug: fct-musicbrainz-release-observation-track-depth
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_musicbrainz_release_observation_observed_at_day
  - fct_musicbrainz_release_observation_average_track_count
tableName: fct_musicbrainz_release_observation
version: 1
