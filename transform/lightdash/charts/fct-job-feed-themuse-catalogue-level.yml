chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_job_feed_themuse_observed_at_day
          yRef:
            field: fct_job_feed_themuse_distinct_entities
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Observed at (Day)
      yAxis:
      - name: Distinct job
    layout:
      xField: fct_job_feed_themuse_observed_at_day
      yField:
      - fct_job_feed_themuse_distinct_entities
  type: cartesian
description: Distinct `job_id` values present in the feed on each observation day. This is a catalogue
  level rather than a count of new postings because the feed republishes listings and repeated observations
  are deduplicated within each day. Days without a successful extract are absent rather than zero.
metricQuery:
  dimensions:
  - fct_job_feed_themuse_observed_at_day
  exploreName: fct_job_feed_themuse
  filters: {}
  limit: 5000
  metrics:
  - fct_job_feed_themuse_distinct_entities
  sorts:
  - descending: false
    fieldId: fct_job_feed_themuse_observed_at_day
  tableCalculations: []
name: Jobs present in the catalogue each day
slug: fct-job-feed-themuse-catalogue-level
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_job_feed_themuse_observed_at_day
  - fct_job_feed_themuse_distinct_entities
tableName: fct_job_feed_themuse
version: 1
