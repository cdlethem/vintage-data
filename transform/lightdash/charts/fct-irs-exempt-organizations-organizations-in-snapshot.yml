chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_irs_exempt_organizations_fetched_at_day
          yRef:
            field: fct_irs_exempt_organizations_distinct_entities
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Fetched at (Day)
      yAxis:
      - name: Distinct organization
    layout:
      xField: fct_irs_exempt_organizations_fetched_at_day
      yField:
      - fct_irs_exempt_organizations_distinct_entities
  type: cartesian
description: Distinct `organization_id` values in the IRS snapshot on each fetch day. This is a level
  rather than a flow because the feed is a catalogue snapshot; only one fetch day is present, and absent
  fetch days are absent rather than zero.
metricQuery:
  dimensions:
  - fct_irs_exempt_organizations_fetched_at_day
  exploreName: fct_irs_exempt_organizations
  filters: {}
  limit: 5000
  metrics:
  - fct_irs_exempt_organizations_distinct_entities
  sorts:
  - descending: false
    fieldId: fct_irs_exempt_organizations_fetched_at_day
  tableCalculations: []
name: Organizations in the fetched snapshot
slug: fct-irs-exempt-organizations-organizations-in-snapshot
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_irs_exempt_organizations_fetched_at_day
  - fct_irs_exempt_organizations_distinct_entities
tableName: fct_irs_exempt_organizations
version: 1
