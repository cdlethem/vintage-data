chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_safecast_radiation_measurement_captured_at_day
          yRef:
            field: fct_safecast_radiation_measurement_average_measurement_value
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Captured at (Day)
      yAxis:
      - name: Mean observed measurement value
    layout:
      xField: fct_safecast_radiation_measurement_captured_at_day
      yField:
      - fct_safecast_radiation_measurement_average_measurement_value
  type: cartesian
description: Mean `measurement_value` by device `captured_at` day. Captured readings span only five days
  in this mart, so this is a short source-event view rather than long-term history; each reading has equal
  weight and an absent captured day is not zero.
metricQuery:
  dimensions:
  - fct_safecast_radiation_measurement_captured_at_day
  exploreName: fct_safecast_radiation_measurement
  filters: {}
  limit: 5000
  metrics:
  - fct_safecast_radiation_measurement_average_measurement_value
  sorts:
  - descending: false
    fieldId: fct_safecast_radiation_measurement_captured_at_day
  tableCalculations: []
name: Mean radiation reading per captured day
slug: fct-safecast-radiation-measurement-captured-level
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_safecast_radiation_measurement_captured_at_day
  - fct_safecast_radiation_measurement_average_measurement_value
tableName: fct_safecast_radiation_measurement
version: 1
