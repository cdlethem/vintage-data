chartConfig:
  config:
    columnLimit: 8
    eChartsConfig:
      legend:
        orient: vertical
        placement: outsideRight
      series:
      - encode:
          xRef:
            field: fct_certificate_transparency_certificate_observation_nb_month
          yRef:
            field: fct_certificate_transparency_certificate_observation_m_a6e4e251
        type: bar
        yAxisIndex: 0
      xAxis:
      - name: Not before (Month)
      yAxis:
      - name: Distinct certificate
    layout:
      stack: true
      xField: fct_certificate_transparency_certificate_observation_nb_month
      yField:
      - fct_certificate_transparency_certificate_observation_m_a6e4e251
  type: cartesian
description: Distinct `certificate_id` values per `not_before` month, stacked by the eight largest issuers
  across the full history — all remaining issuers are excluded rather than aggregated. Sparse issuance
  months are absent, not zero.
metricQuery:
  dimensions:
  - fct_certificate_transparency_certificate_observation_nb_month
  - fct_certificate_transparency_certificate_observation_issuer
  exploreName: fct_certificate_transparency_certificate_observation
  filters: {}
  limit: 5000
  metrics:
  - fct_certificate_transparency_certificate_observation_m_a6e4e251
  sorts:
  - descending: true
    fieldId: fct_certificate_transparency_certificate_observation_m_a6e4e251
  - descending: false
    fieldId: fct_certificate_transparency_certificate_observation_nb_month
  tableCalculations: []
name: Certificates issued by leading issuer
pivotConfig:
  columns:
  - fct_certificate_transparency_certificate_observation_issuer
slug: fct-certificate-transparency-certificate-observation-issuer-composition
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_certificate_transparency_certificate_observation_nb_month
  - fct_certificate_transparency_certificate_observation_issuer
  - fct_certificate_transparency_certificate_observation_m_a6e4e251
tableName: fct_certificate_transparency_certificate_observation
version: 1
