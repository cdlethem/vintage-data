chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_w3c_specification_observation_observed_at_day
          yRef:
            field: fct_w3c_specification_observation_distinct_entities
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Observed at (Day)
      yAxis:
      - name: Distinct specification
    layout:
      xField: fct_w3c_specification_observation_observed_at_day
      yField:
      - fct_w3c_specification_observation_distinct_entities
  type: cartesian
description: Distinct `specification_id` values present in the feed on each observation day. This is a
  catalogue level rather than a publication flow — repeated feed snapshots count each specification once
  per day, and absent days are absent rather than zero.
metricQuery:
  dimensions:
  - fct_w3c_specification_observation_observed_at_day
  exploreName: fct_w3c_specification_observation
  filters: {}
  limit: 5000
  metrics:
  - fct_w3c_specification_observation_distinct_entities
  sorts:
  - descending: false
    fieldId: fct_w3c_specification_observation_observed_at_day
  tableCalculations: []
name: Specifications present per observation day
slug: fct-w3c-specification-observation-specifications-per-day
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_w3c_specification_observation_observed_at_day
  - fct_w3c_specification_observation_distinct_entities
tableName: fct_w3c_specification_observation
version: 1
