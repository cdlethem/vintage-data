chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_huggingface_model_snapshot_observed_at_day
          yRef:
            field: fct_huggingface_model_snapshot_distinct_models
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Observed at (Day)
      yAxis:
      - name: Distinct models
    layout:
      xField: fct_huggingface_model_snapshot_observed_at_day
      yField:
      - fct_huggingface_model_snapshot_distinct_models
  type: cartesian
description: Distinct `model_id` values present in the catalogue on each extractor observation day. This
  is a catalogue level rather than a flow because models recur across snapshots; days without a successful
  extract are absent rather than zero.
metricQuery:
  dimensions:
  - fct_huggingface_model_snapshot_observed_at_day
  exploreName: fct_huggingface_model_snapshot
  filters: {}
  limit: 5000
  metrics:
  - fct_huggingface_model_snapshot_distinct_models
  sorts:
  - descending: false
    fieldId: fct_huggingface_model_snapshot_observed_at_day
  tableCalculations: []
name: Distinct models observed each day
slug: fct-huggingface-model-snapshot-catalogue-size
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_huggingface_model_snapshot_observed_at_day
  - fct_huggingface_model_snapshot_distinct_models
tableName: fct_huggingface_model_snapshot
version: 1
