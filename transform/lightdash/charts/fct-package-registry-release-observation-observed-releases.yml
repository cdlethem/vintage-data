chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_package_registry_release_observation_observed_at_day
          yRef:
            field: fct_package_registry_release_observation_distinct_entities
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Observed at (Day)
      yAxis:
      - name: Distinct release
    layout:
      xField: fct_package_registry_release_observation_observed_at_day
      yField:
      - fct_package_registry_release_observation_distinct_entities
  type: cartesian
description: Distinct `release_id` values present in each extractor observation day. This is a catalogue
  level rather than a publication flow because the registry feed republishes releases; days without a
  successful extract are absent rather than zero.
metricQuery:
  dimensions:
  - fct_package_registry_release_observation_observed_at_day
  exploreName: fct_package_registry_release_observation
  filters: {}
  limit: 5000
  metrics:
  - fct_package_registry_release_observation_distinct_entities
  sorts:
  - descending: false
    fieldId: fct_package_registry_release_observation_observed_at_day
  tableCalculations: []
name: Distinct releases present each observation day
slug: fct-package-registry-release-observation-observed-releases
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_package_registry_release_observation_observed_at_day
  - fct_package_registry_release_observation_distinct_entities
tableName: fct_package_registry_release_observation
version: 1
