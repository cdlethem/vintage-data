chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_gutendex_book_snapshot_fetched_at_day
          yRef:
            field: fct_gutendex_book_snapshot_average_downloads
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Fetched at (Day)
      yAxis:
      - name: Mean downloads
    layout:
      xField: fct_gutendex_book_snapshot_fetched_at_day
      yField:
      - fct_gutendex_book_snapshot_average_downloads
  type: cartesian
description: Average `download_count` across books in each fetch day. This is a publisher- reported snapshot
  measure with each retained book observation equally weighted, not a sum of downloads or a cumulative
  flow; absent fetch days are absent rather than zero.
metricQuery:
  dimensions:
  - fct_gutendex_book_snapshot_fetched_at_day
  exploreName: fct_gutendex_book_snapshot
  filters: {}
  limit: 5000
  metrics:
  - fct_gutendex_book_snapshot_average_downloads
  sorts:
  - descending: false
    fieldId: fct_gutendex_book_snapshot_fetched_at_day
  tableCalculations: []
name: Mean reported downloads per fetched book
slug: fct-gutendex-book-snapshot-downloads-by-fetch-day
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_gutendex_book_snapshot_fetched_at_day
  - fct_gutendex_book_snapshot_average_downloads
tableName: fct_gutendex_book_snapshot
version: 1
