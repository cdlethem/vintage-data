chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_gh_archive_events_fetched_at_day
          yRef:
            field: fct_gh_archive_events_distinct_entities
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Fetched at (Day)
      yAxis:
      - name: Distinct event
    layout:
      xField: fct_gh_archive_events_fetched_at_day
      yField:
      - fct_gh_archive_events_distinct_entities
  type: cartesian
description: Distinct `event_id` values in each extractor fetch day. This is a collection-level view of
  a five-day feed window, not source event history; days without a successful extract are absent rather
  than zero.
metricQuery:
  dimensions:
  - fct_gh_archive_events_fetched_at_day
  exploreName: fct_gh_archive_events
  filters: {}
  limit: 5000
  metrics:
  - fct_gh_archive_events_distinct_entities
  sorts:
  - descending: false
    fieldId: fct_gh_archive_events_fetched_at_day
  tableCalculations: []
name: Distinct events fetched per day
slug: fct-gh-archive-events-fetched-event-level
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_gh_archive_events_fetched_at_day
  - fct_gh_archive_events_distinct_entities
tableName: fct_gh_archive_events
version: 1
