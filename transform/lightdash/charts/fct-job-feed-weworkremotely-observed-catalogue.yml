chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_job_feed_weworkremotely_fetched_at_day
          yRef:
            field: fct_job_feed_weworkremotely_distinct_entities
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Fetched at (Day)
      yAxis:
      - name: Distinct job
    layout:
      xField: fct_job_feed_weworkremotely_fetched_at_day
      yField:
      - fct_job_feed_weworkremotely_distinct_entities
  type: cartesian
description: Distinct `job_id` values present when the extractor fetched the feed each day. This is a
  catalogue level rather than a new-job flow because the feed repeats live listings; days without a successful
  fetch are absent rather than zero.
metricQuery:
  dimensions:
  - fct_job_feed_weworkremotely_fetched_at_day
  exploreName: fct_job_feed_weworkremotely
  filters: {}
  limit: 5000
  metrics:
  - fct_job_feed_weworkremotely_distinct_entities
  sorts:
  - descending: false
    fieldId: fct_job_feed_weworkremotely_fetched_at_day
  tableCalculations: []
name: Distinct jobs present by fetch day
slug: fct-job-feed-weworkremotely-observed-catalogue
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_job_feed_weworkremotely_fetched_at_day
  - fct_job_feed_weworkremotely_distinct_entities
tableName: fct_job_feed_weworkremotely
version: 1
