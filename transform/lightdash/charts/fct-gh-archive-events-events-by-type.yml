chartConfig:
  config:
    columnLimit: 6
    eChartsConfig:
      legend:
        orient: vertical
        placement: outsideRight
      series:
      - encode:
          xRef:
            field: fct_gh_archive_events_created_at_day
          yRef:
            field: fct_gh_archive_events_distinct_entities
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Created at (Day)
      yAxis:
      - name: Distinct event
    layout:
      xField: fct_gh_archive_events_created_at_day
      yField:
      - fct_gh_archive_events_distinct_entities
  type: cartesian
description: Distinct `event_id` values per creation day, pivoted by `event_type`. The six observed types
  are retained; no remainder is aggregated, and sparse types may be absent on individual days rather than
  zero.
metricQuery:
  dimensions:
  - fct_gh_archive_events_created_at_day
  - fct_gh_archive_events_event_type
  exploreName: fct_gh_archive_events
  filters: {}
  limit: 5000
  metrics:
  - fct_gh_archive_events_distinct_entities
  sorts:
  - descending: true
    fieldId: fct_gh_archive_events_distinct_entities
  - descending: false
    fieldId: fct_gh_archive_events_created_at_day
  tableCalculations: []
name: Event volume by GitHub event type
pivotConfig:
  columns:
  - fct_gh_archive_events_event_type
slug: fct-gh-archive-events-events-by-type
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_gh_archive_events_created_at_day
  - fct_gh_archive_events_event_type
  - fct_gh_archive_events_distinct_entities
tableName: fct_gh_archive_events
version: 1
