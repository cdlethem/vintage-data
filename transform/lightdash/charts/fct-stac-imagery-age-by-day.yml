chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_stac_imagery_observed_at_day
          yRef:
            field: fct_stac_imagery_average_age_hours
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Observed at (Day)
      yAxis:
      - name: Mean imagery age hours
    layout:
      xField: fct_stac_imagery_observed_at_day
      yField:
      - fct_stac_imagery_average_age_hours
  type: cartesian
description: Mean reported `age_hours` for items at each extractor observation day. This is an observation-weighted
  level rather than a flow or cumulative age; days with no extract are absent rather than zero.
metricQuery:
  dimensions:
  - fct_stac_imagery_observed_at_day
  exploreName: fct_stac_imagery
  filters: {}
  limit: 5000
  metrics:
  - fct_stac_imagery_average_age_hours
  sorts:
  - descending: false
    fieldId: fct_stac_imagery_observed_at_day
  tableCalculations: []
name: Mean imagery age at observation
slug: fct-stac-imagery-age-by-day
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_stac_imagery_observed_at_day
  - fct_stac_imagery_average_age_hours
tableName: fct_stac_imagery
version: 1
