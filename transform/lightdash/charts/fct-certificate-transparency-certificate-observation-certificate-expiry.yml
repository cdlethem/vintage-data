chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_certificate_transparency_certificate_observation_na_month
          yRef:
            field: fct_certificate_transparency_certificate_observation_m_a6e4e251
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Not after (Month)
      yAxis:
      - name: Distinct certificate
    layout:
      xField: fct_certificate_transparency_certificate_observation_na_month
      yField:
      - fct_certificate_transparency_certificate_observation_m_a6e4e251
  type: cartesian
description: Distinct `certificate_id` values by certificate `not_after` month — this is an expiry-date
  flow based on publisher validity fields, not certificates actually seen expiring. Months without certificates
  ending are absent rather than zero.
metricQuery:
  dimensions:
  - fct_certificate_transparency_certificate_observation_na_month
  exploreName: fct_certificate_transparency_certificate_observation
  filters: {}
  limit: 5000
  metrics:
  - fct_certificate_transparency_certificate_observation_m_a6e4e251
  sorts:
  - descending: false
    fieldId: fct_certificate_transparency_certificate_observation_na_month
  tableCalculations: []
name: Certificates expiring per month
slug: fct-certificate-transparency-certificate-observation-certificate-expiry
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_certificate_transparency_certificate_observation_na_month
  - fct_certificate_transparency_certificate_observation_m_a6e4e251
tableName: fct_certificate_transparency_certificate_observation
version: 1
