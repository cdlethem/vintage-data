chartConfig:
  config:
    columnLimit: 8
    eChartsConfig:
      legend:
        orient: vertical
        placement: outsideRight
      series:
      - encode:
          xRef:
            field: fct_package_registry_release_observation_published_at_day
          yRef:
            field: fct_package_registry_release_observation_distinct_entities
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Published at (Day)
      yAxis:
      - name: Distinct release
    layout:
      xField: fct_package_registry_release_observation_published_at_day
      yField:
      - fct_package_registry_release_observation_distinct_entities
  type: cartesian
description: Distinct `release_id` values by non-null `author` and publication day. The eight authors
  with the most releases are shown and the remainder is excluded rather than aggregated; releases without
  an author are omitted and missing dates are absent rather than zero.
metricQuery:
  dimensions:
  - fct_package_registry_release_observation_published_at_day
  - fct_package_registry_release_observation_author
  exploreName: fct_package_registry_release_observation
  filters: {}
  limit: 5000
  metrics:
  - fct_package_registry_release_observation_distinct_entities
  sorts:
  - descending: true
    fieldId: fct_package_registry_release_observation_distinct_entities
  - descending: false
    fieldId: fct_package_registry_release_observation_published_at_day
  tableCalculations: []
name: Published releases by leading author
pivotConfig:
  columns:
  - fct_package_registry_release_observation_author
slug: fct-package-registry-release-observation-published-author-bursts
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_package_registry_release_observation_published_at_day
  - fct_package_registry_release_observation_author
  - fct_package_registry_release_observation_distinct_entities
tableName: fct_package_registry_release_observation
version: 1
