chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_gutendex_book_snapshot_fetched_at_day
          yRef:
            field: fct_gutendex_book_snapshot_distinct_entities
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Fetched at (Day)
      yAxis:
      - name: Distinct book
    layout:
      xField: fct_gutendex_book_snapshot_fetched_at_day
      yField:
      - fct_gutendex_book_snapshot_distinct_entities
  type: cartesian
description: Distinct `book_id` values present in each Gutendex fetch day. This is a catalog level rather
  than a publication flow because the publisher supplies no source event date; days without a successful
  fetch are absent rather than zero.
metricQuery:
  dimensions:
  - fct_gutendex_book_snapshot_fetched_at_day
  exploreName: fct_gutendex_book_snapshot
  filters: {}
  limit: 5000
  metrics:
  - fct_gutendex_book_snapshot_distinct_entities
  sorts:
  - descending: false
    fieldId: fct_gutendex_book_snapshot_fetched_at_day
  tableCalculations: []
name: Distinct books in each daily snapshot
slug: fct-gutendex-book-snapshot-books-by-fetch-day
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_gutendex_book_snapshot_fetched_at_day
  - fct_gutendex_book_snapshot_distinct_entities
tableName: fct_gutendex_book_snapshot
version: 1
