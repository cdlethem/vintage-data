chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_netflix_top10_fetched_at_day
          yRef:
            field: fct_netflix_top10_average_weekly_hours_viewed
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Fetched at (Day)
      yAxis:
      - name: Mean observed weekly hours viewed
    layout:
      xField: fct_netflix_top10_fetched_at_day
      yField:
      - fct_netflix_top10_average_weekly_hours_viewed
  type: cartesian
description: Mean publisher-reported `weekly_hours_viewed` across titles in each daily fetch. Each title
  row has equal weight, so this is a cross-title average of a ranking-week measure, not a summed audience
  total; absent fetch days are absent rather than zero.
metricQuery:
  dimensions:
  - fct_netflix_top10_fetched_at_day
  exploreName: fct_netflix_top10
  filters: {}
  limit: 5000
  metrics:
  - fct_netflix_top10_average_weekly_hours_viewed
  sorts:
  - descending: false
    fieldId: fct_netflix_top10_fetched_at_day
  tableCalculations: []
name: Mean weekly hours viewed per fetch day
slug: fct-netflix-top10-viewing-hours
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_netflix_top10_fetched_at_day
  - fct_netflix_top10_average_weekly_hours_viewed
tableName: fct_netflix_top10
version: 1
