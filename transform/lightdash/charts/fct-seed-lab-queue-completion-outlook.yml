chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_seed_lab_queue_observed_at_day
          yRef:
            field: fct_seed_lab_queue_average_estimated_days_to_complete
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Observed at (Day)
      yAxis:
      - name: Mean observed estimated days to complete
    layout:
      xField: fct_seed_lab_queue_observed_at_day
      yField:
      - fct_seed_lab_queue_average_estimated_days_to_complete
  type: cartesian
description: Average reported `estimated_days_to_complete` on each snapshot day. Only the completion-estimate
  rows populate this measure, so other queue rows and days with no estimate are absent rather than zero.
metricQuery:
  dimensions:
  - fct_seed_lab_queue_observed_at_day
  exploreName: fct_seed_lab_queue
  filters: {}
  limit: 5000
  metrics:
  - fct_seed_lab_queue_average_estimated_days_to_complete
  sorts:
  - descending: false
    fieldId: fct_seed_lab_queue_observed_at_day
  tableCalculations: []
name: Estimated completion days by snapshot day
slug: fct-seed-lab-queue-completion-outlook
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_seed_lab_queue_observed_at_day
  - fct_seed_lab_queue_average_estimated_days_to_complete
tableName: fct_seed_lab_queue
version: 1
