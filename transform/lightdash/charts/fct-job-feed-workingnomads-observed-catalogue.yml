chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_job_feed_workingnomads_observed_at_day
          yRef:
            field: fct_job_feed_workingnomads_distinct_entities
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Observed at (Day)
      yAxis:
      - name: Distinct job
    layout:
      xField: fct_job_feed_workingnomads_observed_at_day
      yField:
      - fct_job_feed_workingnomads_distinct_entities
  type: cartesian
description: Distinct `job_id` values present when the extractor observed the feed each day. This is a
  catalogue level rather than a new-job flow because listings recur across fetches; days without a successful
  observation are absent rather than zero.
metricQuery:
  dimensions:
  - fct_job_feed_workingnomads_observed_at_day
  exploreName: fct_job_feed_workingnomads
  filters: {}
  limit: 5000
  metrics:
  - fct_job_feed_workingnomads_distinct_entities
  sorts:
  - descending: false
    fieldId: fct_job_feed_workingnomads_observed_at_day
  tableCalculations: []
name: Distinct jobs present by observation day
slug: fct-job-feed-workingnomads-observed-catalogue
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_job_feed_workingnomads_observed_at_day
  - fct_job_feed_workingnomads_distinct_entities
tableName: fct_job_feed_workingnomads
version: 1
