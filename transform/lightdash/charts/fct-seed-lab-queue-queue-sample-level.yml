chartConfig:
  config:
    eChartsConfig:
      series:
      - encode:
          xRef:
            field: fct_seed_lab_queue_observed_at_day
          yRef:
            field: fct_seed_lab_queue_average_sample_count
        type: line
        yAxisIndex: 0
      xAxis:
      - name: Observed at (Day)
      yAxis:
      - name: Mean observed sample count
    layout:
      xField: fct_seed_lab_queue_observed_at_day
      yField:
      - fct_seed_lab_queue_average_sample_count
  type: cartesian
description: Average `sample_count` across queue rows on each source snapshot day. This is a publisher-reported
  snapshot level rather than a flow or additive total; duplicate rows in a retained snapshot are observation-weighted,
  and absent snapshot days are absent rather than zero.
metricQuery:
  dimensions:
  - fct_seed_lab_queue_observed_at_day
  exploreName: fct_seed_lab_queue
  filters: {}
  limit: 5000
  metrics:
  - fct_seed_lab_queue_average_sample_count
  sorts:
  - descending: false
    fieldId: fct_seed_lab_queue_observed_at_day
  tableCalculations: []
name: Mean reported sample count by snapshot day
slug: fct-seed-lab-queue-queue-sample-level
spaceSlug: vintage-marts
tableConfig:
  columnOrder:
  - fct_seed_lab_queue_observed_at_day
  - fct_seed_lab_queue_average_sample_count
tableName: fct_seed_lab_queue
version: 1
